server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript;

    # SPA fallback: any unknown route should serve index.html for client-side routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Internal Healthcheck
    location = /healthz {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }
}
